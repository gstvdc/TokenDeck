# PlatformIO build artifacts
firmware/.pio/
firmware/.vscode/
firmware/compile_commands.json

# OS
.DS_Store

# Claude Code
.claude/
.agents/
skills-lock.json

# Node tooling deps (npm install --no-save)
tools/node_modules/
tools/package-lock.json

# Mac daemon venv
daemon/.venv/
daemon/.gemini-cli/
daemon/.gemini-state/

# Python bytecode cache
__pycache__/
*.pyc

# Machine-specific shortcut created by create-shortcut.ps1
TokenDeck.lnk

# Runtime state written by the app/bridge, not project config
daemon/device_config.json
daemon/history.jsonl
daemon/latest_status.json
